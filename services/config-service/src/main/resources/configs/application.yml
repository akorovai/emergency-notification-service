eureka:
  instance:
    hostname:
      localhost:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
name:
  value: emergency-notification-service

spring:
  cloud:
    config:
      override-system-properties: false