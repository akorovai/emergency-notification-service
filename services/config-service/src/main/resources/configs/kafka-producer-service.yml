spring:
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: com.da.kafkaproducerservice.serializer.EmailMessageSerializer
    topic:
      name: "main-topic"
      partitions: 6

server:
  port: 8765